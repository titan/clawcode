/* ClawCode Yellow Theme */

/* Global settings */
Screen {
    background: #1a1a1a;
}

/* Header */
Header {
    background: #2a2a2a;
    text-align: center;
    text-style: bold;
    dock: top;
    height: 1;
    padding: 0 1;
}

Header.-tint {
    text-style: bold;
    background: #2a2a2a;
}

/* Footer */
Footer {
    background: #2a2a2a;
    text-align: center;
    dock: bottom;
    padding: 0 1;
}

/* Main container */
#main_container {
    height: 100%;
    width: 100%;
}

/* Chat container */
#chat_container {
    height: 1fr;
    width: 1fr;
}

/* Sidebar */
#sidebar {
    width: 25;
    min-width: 20;
    max-width: 40;
    dock: left;
    background: #2a2a2a;
    border-right: thick #4a4a3a;
}

/* Chat area */
#chat_area {
    width: 1fr;
    height: 1fr;
}

/* Message list */
#message_list {
    height: 1fr;
    width: 1fr;
    padding: 1;
    background: #1a1a1a;
}

/* Welcome message */
#welcome_message {
    width: 100%;
    height: auto;
    margin: 0;
    padding: 0 0;
}

/* Input area */
#message_input {
    dock: bottom;
    height: auto;
    margin: 1;
    background: #1a1a1a;
    border: solid #4a4a3a;
    padding: 1;
}

/* Input help */
.input_help {
    dock: bottom;
    height: 3;
    min-height: 3;
    color: #8b8b7a;
    padding: 0 1;
    text-align: right;
    overflow: hidden;
}

/* Individual message */
.message {
    margin: 1 0;
    padding: 1;
    background: #2a2a2a;
    border: solid #4a4a3a;
    border-title-align: left;
    border-title-color: #8b8b7a;
    border-subtitle-color: #8b8b7a;
}

.message.user {
    border-title-color: #ffd700;
    border-title-style: bold;
}

.message.assistant {
    border-title-color: #ffb347;
    border-title-style: bold;
}

.message.system {
    border-title-color: #9acd32;
    border-title-style: bold;
}

.message.tool {
    background: transparent;
    border: none;
    padding: 0 2;
}

/* Message content */
.message_content {
    margin-top: 1;
    text-style: none;
}

/* Thinking content */
.thinking_content {
    color: #8b8b7a;
    text-style: italic;
    margin-top: 1;
    padding: 0 2;
}

/* Tool call display */
.tool_call {
    background: #2a2a2a;
    border: solid #4a4a3a;
    padding: 0 1;
    margin: 0 2;
}

.tool_call_name {
    color: #ffb347;
    text-style: bold;
}

.tool_call_input {
    color: #8b8b7a;
    margin-left: 2;
}

/* Tool result display */
.tool_result {
    background: #2a2a2a;
    border: solid #4a4a3a;
    padding: 0 1;
    margin: 0 2;
}

.tool_result_content {
    margin-top: 1;
    padding: 0 2;
}

/* Input area */
#input_area {
    dock: bottom;
    height: auto;
    padding: 0 1;
    background: #2a2a2a;
    border-top: thick #4a4a3a;
}

/* Input field */
#message_input {
    margin: 1 0;
}

/* Input help text */
.input_help {
    height: 3;
    min-height: 3;
    align: right middle;
    color: #8b8b7a;
    padding: 0 1;
    overflow: hidden;
}

/* Buttons */
Button {
    background: #ffd700;
    color: #1a1a1a;
    text-style: bold;
    border: none;
    padding: 0 2;
    margin: 0 1;
}

Button:hover {
    background: #ffb347;
}

Button.-primary {
    background: #ffd700;
}

Button.-error {
    background: #ff6b6b;
}

Button.-success {
    background: #9acd32;
}

/* Permission dialog */
#permission_dialog {
    width: 80%;
    height: auto;
    max-height: 20;
    background: #2a2a2a;
    border: thick #4a4a3a;
    padding: 1;
}

.permission_header {
    text-style: bold;
    text-align: center;
    color: #f5c542;
    margin-bottom: 1;
}

.permission_description {
    margin: 1 0;
}

.permission_path {
    color: #ffb347;
    text-style: bold;
    margin: 1 0;
}

.permission_buttons {
    height: 3;
    align: center middle;
    margin-top: 1;
}

/* Help screen */
#help_screen {
    padding: 2;
}

.help_section {
    margin: 1 0;
}

.help_title {
    color: #ffd700;
    text-style: bold;
    margin-bottom: 1;
}

.help_key {
    color: #ffb347;
    text-style: bold;
}

.help_description {
    margin-left: 2;
    color: #f5f5f0;
}

/* Logs screen */
#logs_content {
    height: 1fr;
    overflow-y: auto;
    padding: 1;
}

.log_entry {
    margin: 0 0;
    padding: 0 1;
}

.log_entry.debug {
    color: #8b8b7a;
}

.log_entry.info {
    color: #f5f5f0;
}

.log_entry.warning {
    color: #ffa500;
}

.log_entry.error {
    color: #ff6b6b;
}

/* Session list */
#session_list {
    height: 1fr;
    overflow-y: auto;
}

.session_item {
    padding: 1;
    margin: 0 1;
}

.session_item:hover {
    background: #ffd700;
    color: #1a1a1a;
}

.session_item.selected {
    background: #ffb347;
    color: #1a1a1a;
    text-style: bold;
}

/* Loading indicator */
.loading {
    color: #ffb347;
    text-style: bold;
}

/* Code blocks */
.code_block {
    background: #1a1a1a;
    border: solid #4a4a3a;
    padding: 1;
    margin: 1 0;
    text-style: none;
}

.code_language {
    color: #8b8b7a;
    text-style: italic;
    margin-bottom: 1;
}

/* Scrollbar */
ScrollView > VerticalScrollbar {
    background: #2a2a2a;
    color: #8b8b7a;
}

ScrollView > VerticalScrollbar:hover {
    background: #4a4a3a;
    color: #f5f5f0;
}

ScrollView > VerticalScrollbar > .thumb {
    background: #8b8b7a;
}

ScrollView > VerticalScrollbar:hover > .thumb {
    background: #f5f5f0;
}

/* Session dialog */
#session_dialog {
    width: 80%;
    height: 80%;
    background: #2a2a2a;
    border: thick #4a4a3a;
    padding: 1;
}

.dialog_header {
    text-style: bold;
    text-align: center;
    color: #ffd700;
    margin-bottom: 1;
}

.dialog_input {
    margin: 1 0;
}

/* Model dialog */
#model_dialog {
    width: 80%;
    height: 80%;
    background: #2a2a2a;
    border: thick #4a4a3a;
    padding: 1;
}

.dialog_info {
    color: #8b8b7a;
    margin: 1 0;
}

/* Commands dialog */
#commands_dialog {
    width: 80%;
    height: 80%;
    background: #2a2a2a;
    border: thick #4a4a3a;
    padding: 1;
}

.command_item {
    padding: 0 1;
}

/* Dialog buttons */
#session_buttons,
#model_buttons,
#commands_buttons {
    height: 3;
    align: center middle;
    margin-top: 1;
}
